# TODO

- [x] Split inline CSS from the provided tickets HTML into `css/style.css` (append at end, keep existing styles intact).

- [x] Split inline JS from the provided tickets HTML into `js/script.js` (append at end, keep existing JS intact).


- [x] Replace `tickets.html` with the provided NoSQL Ticket Studio page structure (will keep site includes).

- [ ] Ensure IDs/classes referenced in JS exist in the updated `tickets.html`.
- [x] Fix any naming conflicts with existing code in `js/script.js` (e.g., dark mode toggle already exists).


- [x] Test in browser: IndexedDB init, save/edit/delete, filtering, CSV export, dark mode persistence.




